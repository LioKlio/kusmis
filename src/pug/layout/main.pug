include ../mixins/mixins.pug

block vars

doctype html
html(lang="ru")
  head
    meta(charset="UTF-8")
    meta(http-equiv="X-UA-Compatible", content="ie=edge")
    meta(name="viewport", content="width=device-width, initial-scale=1.0, shrink-to-fit=no")

    title= title

    block styles

    include _l1_styles.pug

  body(class= pagetype)

    include _l2_header.pug

    block content

    include _l3_footer.pug

    block scripts

    include _l4_scripts.pug
